import { sql } from "drizzle-orm";
import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const leads = sqliteTable("leads", { id:text("id").primaryKey(), createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), flow:text("flow").notNull(), service:text("service").notNull(), city:text("city"), zip:text("zip"), name:text("name").notNull(), phone:text("phone").notNull(), email:text("email"), contactMethod:text("contact_method"), payloadJson:text("payload_json").notNull(), landingPage:text("landing_page"), pageUrl:text("page_url"), referrer:text("referrer"), utmSource:text("utm_source"), utmMedium:text("utm_medium"), utmCampaign:text("utm_campaign"), utmTerm:text("utm_term"), utmContent:text("utm_content"), channel:text("channel"), score:integer("score").notNull(), scoreLabel:text("score_label").notNull(), status:text("status").notNull().default("new"), consent:integer("consent").notNull().default(0) });
